<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.oa.police.form.dao.PoliceFormTemplateDao">

    <!-- 根据警情类型和组织ID获取模板 -->
    <select id="getTemplateByTypeAndOrg" resultType="net.lab1024.sa.admin.module.business.oa.police.form.domain.entity.PoliceFormTemplateEntity">
        SELECT * FROM t_police_form_template
        WHERE report_type = #{reportType}
          AND status = 1
          AND (
            organization_id = #{organizationId}
            OR (organization_id IS NULL AND is_default = 1)
          )
        ORDER BY organization_id DESC, is_default DESC
        LIMIT 1
    </select>

</mapper>