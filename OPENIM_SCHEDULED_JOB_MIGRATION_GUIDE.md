# OpenIM定时任务迁移指南

## 概述

本次更新将OpenIM用户同步定时任务从`@Scheduled`注解方式迁移到SmartAdmin的定时任务管理模块，实现了界面化配置和管理。

## 版本信息

- **版本**: v3.28.0
- **日期**: 2025-10-11
- **影响范围**: OpenIM用户同步相关定时任务

---

## 迁移内容

### 1. 新增SmartJob任务类

#### 1.1 OpenIM用户增量同步任务

**类名**: `IMUserIncrementalSyncJob`
**路径**: `net.lab1024.sa.admin.module.support.im.job.IMUserIncrementalSyncJob`

**功能**:
- 定时增量同步SmartAdmin员工到OpenIM系统
- 监控同步状态和统计
- 异常告警

**默认配置**:
- 触发方式: Cron表达式
- 执行频率: 每小时执行一次 (`0 0 * * * ?`)

#### 1.2 OpenIM用户同步每日报告任务

**类名**: `IMUserDailySyncReportJob`
**路径**: `net.lab1024.sa.admin.module.support.im.job.IMUserDailySyncReportJob`

**功能**:
- 每日打印同步统计报告
- 可通过定时任务执行日志查看详细数据

**默认配置**:
- 触发方式: Cron表达式
- 执行频率: 每天凌晨2点 (`0 0 2 * * ?`)

### 2. 数据库初始化

**SQL脚本**: `sql/sql-update-log/v3.28.0-openim-scheduled-job.sql`

该脚本会在`t_smart_job`表中插入两条任务配置记录。

### 3. 已移除的代码

- **删除**: `IMUserScheduledSyncService.java` (旧的定时任务服务类)
- **移除**: `IMUserController` 中与旧定时任务相关的API接口
  - `GET /api/im/user/sync/stats` (已删除)
  - `POST /api/im/user/sync/trigger-incremental` (已删除)
  - `POST /api/im/user/sync/trigger-full` (已删除)

---

## 部署步骤

### 步骤1: 执行数据库脚本

```sql
-- 执行以下SQL脚本
source sql/sql-update-log/v3.28.0-openim-scheduled-job.sql;
```

或者手动在数据库中执行SQL脚本内容。

### 步骤2: 重启应用

```bash
# 重新编译
cd smart-admin-api-java17-springboot3
mvn clean package -DskipTests

# 重启应用
# (根据你的部署方式重启)
```

### 步骤3: 验证任务配置

1. 登录SmartAdmin管理后台
2. 导航到 **系统管理 -> 定时任务**
3. 确认以下两个任务已成功添加:
   - OpenIM用户增量同步
   - OpenIM用户同步每日报告

---

## 使用说明

### 界面化管理定时任务

#### 1. 查看任务列表

路径: **系统管理 -> 定时任务**

可以看到所有定时任务，包括新增的OpenIM同步任务。

#### 2. 修改执行频率

1. 点击任务右侧的 **编辑** 按钮
2. 修改 **触发配置** 字段 (Cron表达式)
3. 保存

**常用Cron表达式示例**:
- 每小时: `0 0 * * * ?`
- 每30分钟: `0 0/30 * * * ?`
- 每15分钟: `0 0/15 * * * ?`
- 每天凌晨2点: `0 0 2 * * ?`
- 每周一凌晨2点: `0 0 2 ? * MON`

#### 3. 启用/禁用任务

1. 点击任务右侧的 **开关按钮**
2. 确认操作

#### 4. 手动执行任务

1. 点击任务右侧的 **执行** 按钮
2. 任务会立即执行一次
3. 执行结果会记录到执行日志中

#### 5. 查看执行日志

1. 点击任务右侧的 **日志** 按钮
2. 可以查看该任务的所有执行记录
3. 包括执行时间、执行结果、执行耗时等信息

---

## 功能对比

### 迁移前 (@Scheduled注解方式)

**优点**:
- 配置简单，直接在代码中注解

**缺点**:
- 修改执行频率需要改代码并重启应用
- 无法动态启用/禁用
- 执行日志不完善
- 无界面化管理

### 迁移后 (SmartJob方式)

**优点**:
- ✅ 界面化配置和管理
- ✅ 动态修改执行频率 (无需重启)
- ✅ 动态启用/禁用
- ✅ 完善的执行日志记录
- ✅ 手动触发执行
- ✅ 统一的任务管理中心
- ✅ 支持任务执行监控和告警

**缺点**:
- 需要在数据库中配置

---

## API变更说明

### 已移除的API

以下API已被移除，建议使用SmartAdmin定时任务管理界面替代:

| 旧API | 方法 | 替代方案 |
|-------|------|---------|
| `/api/im/user/sync/stats` | GET | 查看定时任务执行日志 |
| `/api/im/user/sync/trigger-incremental` | POST | 使用定时任务界面手动执行 |
| `/api/im/user/sync/trigger-full` | POST | 使用 `/api/im/user/sync/all` |

### 保留的API

以下API仍然可用于程序化调用同步功能:

| API | 方法 | 说明 |
|-----|------|------|
| `/api/im/user/sync/{employeeId}` | POST | 同步单个用户 |
| `/api/im/user/sync/batch` | POST | 批量同步用户 |
| `/api/im/user/sync/all` | POST | 同步所有用户(异步) |
| `/api/im/user/sync/incremental` | POST | 增量同步 |

---

## 注意事项

### 1. 兼容性

- 迁移后不影响现有的同步功能
- 所有同步相关的API (除定时任务监控API外) 保持不变

### 2. 配置文件

以下配置文件中的配置不再使用，但保留以避免启动错误:
- `openim.sync.enabled`
- `openim.sync.cron`

建议在后续版本中移除这些配置。

### 3. 数据迁移

- 旧的定时任务服务中的统计数据 (总数、成功数、失败数等) 不会自动迁移
- 新系统从零开始统计
- 详细的执行记录可在 `t_smart_job_log` 表中查询

### 4. 监控和告警

- 任务执行失败会在日志中记录
- 建议配置告警系统 (邮件、短信、企业微信等)
- 告警功能预留在Job类中，标记为 `// TODO`

---

## 常见问题

### Q1: 迁移后定时任务不执行？

**A**: 请检查:
1. 数据库中任务配置是否正确插入
2. 任务的 `enabled_flag` 是否为 `1` (启用状态)
3. Cron表达式是否正确
4. 查看应用日志是否有异常

### Q2: 如何查看任务执行历史？

**A**:
1. 进入 **系统管理 -> 定时任务**
2. 点击任务右侧的 **日志** 按钮
3. 查看所有执行记录

### Q3: 如何临时禁用同步任务？

**A**:
1. 进入 **系统管理 -> 定时任务**
2. 找到 "OpenIM用户增量同步" 任务
3. 点击 **开关按钮** 禁用
4. 需要恢复时再次点击开启

### Q4: 任务执行失败如何排查？

**A**:
1. 查看任务执行日志中的错误信息
2. 查看应用日志文件
3. 检查OpenIM服务是否正常
4. 检查数据库连接是否正常

### Q5: 可以同时运行多个增量同步任务吗？

**A**:
不建议。任务调度器会按照配置的频率执行，如果上次执行未完成，会等待完成后再执行下次任务。

---

## 技术细节

### SmartJob执行流程

```
1. SmartJobScheduler 定时扫描 t_smart_job 表
2. 根据 trigger_type 和 trigger_value 判断是否需要执行
3. 调用 SmartJobExecutor 执行任务
4. SmartJobExecutor 通过反射创建Job实例
5. 调用 Job.run() 方法执行业务逻辑
6. 记录执行结果到 t_smart_job_log 表
7. 更新 t_smart_job 表的 last_execute_time 和 last_execute_log_id
```

### Cron表达式格式

SmartAdmin使用标准的6位Cron表达式:

```
格式: 秒 分 时 日 月 周
示例: 0 0 * * * ?

字段说明:
- 秒: 0-59
- 分: 0-59
- 时: 0-23
- 日: 1-31
- 月: 1-12 或 JAN-DEC
- 周: 1-7 或 SUN-SAT (1=周日)

特殊字符:
- *: 任意值
- ?: 不指定值 (仅用于日和周)
- /: 步进值 (如 0/15 表示每15分钟)
- -: 区间 (如 1-5)
- ,: 枚举 (如 1,3,5)
```

---

## 后续优化建议

### 1. 增强监控能力

- [ ] 集成企业微信/钉钉告警
- [ ] 添加同步成功率监控
- [ ] 添加执行耗时监控
- [ ] 自动重试失败的同步

### 2. 数据统计增强

- [ ] 添加同步统计仪表板
- [ ] 添加历史趋势分析
- [ ] 导出统计报表

### 3. 性能优化

- [ ] 大批量同步时的分页处理
- [ ] 并发同步控制
- [ ] 失败重试策略

---

## 联系支持

如有问题，请联系:
- 项目文档: https://smartadmin.vip
- GitHub Issues: https://github.com/1024-lab/smart-admin

---

**更新日期**: 2025-10-11
**文档版本**: v1.0.0
